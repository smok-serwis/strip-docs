version: 2.1
jobs:
  build:
    docker:
      - image: smokserwis/build
    steps:
      - checkout
      - run:
          command: |
            sudo pip install nose2
            sudo python setup.py install
            sudo nose2 -vv
          name: Install and test
