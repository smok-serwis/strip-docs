version: 2.1
jobs:
  build:
    docker:
      - image: smokserwis/build
    steps:
      - checkout
      - run:
          command: |
            pip install nose2
            python setup.py install
            nose2 -vv
          name: Install and test
