version: 2.1
jobs:
  build:
    docker:
     - image: themattrix/tox
    steps:
      - checkout
      - run:
          command: |
            sudo pip install tox
            sudo tox
          name: Install and test
